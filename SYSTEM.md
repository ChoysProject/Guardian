# Guardian 시스템 개요

개인·소규모 운영자가 여러 서버에 앱·웹을 띄울 때, 로그와 성능을 한곳에서 보고 빨리 대응하기 위한 감시 시스템입니다.

Python 웹 서비스로 기동하며, 탐지는 규칙 플러그인이 하고 AI는 추린 결과만 총평합니다.

---

## 1. 하는 일

- **로그 감시**: 등록한 서버에서 로그를 읽어 이상징후(Finding)를 남긴다.
- **리소스 감시**: CPU·메모리·디스크·프로세스 생존을 모아 상태로 보여 준다.
- **보고서**: 일일·서버별 문서를 만들고, 켜 두면 AI 총평을 붙인다.

## 2. 동작 흐름

```
서버 등록 (local / SSH)
        ↓
로그 수집 · 리소스 수집
        ↓
플러그인으로 징후·상태 판정
        ↓
감시 현황 / 징후 목록
        ↓
보고서 (+ AI 총평, 선택)
```

## 3. 구성

| 구분 | 내용 |
| --- | --- |
| 화면 | FastAPI + Jinja2, `http://127.0.0.1:8080` |
| 저장 | SQLite (`data/guardian.db`), 보고서 파일 (`data/reports/`) |
| 로그 플러그인 | 1단계 공통 → 2단계 세부 → 3단계 보고서 |
| 리소스 플러그인 | 4단계 쉘 스크립트 (수집 항목·인스턴스 검색) |
| AI | OpenAI 또는 Dify. 원본 로그 전체가 아니라 요약만 전달. 꺼져 있어도 규칙 분석은 유지 |

## 4. 이후에 채울 항목

아래는 빈 칸입니다. 다음에 어떤 절부터 상세히 넣을지 알려 주세요.

- [ ] 왜 만들었는지 (배경·대상 사용자)
- [ ] 로그 수집 대상 등록 방법
- [ ] 리소스 수집 대상 등록 방법
- [ ] 플러그인 단계별 설명
- [ ] 시연 시나리오 (정상 서버 / 이상 서버)
- [ ] AI 총평이 붙는 시점
- [ ] 데이터·설정 파일 위치
