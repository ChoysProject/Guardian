# Guardian 시스템 개요

개인·소규모 운영자가 여러 서버에 앱·웹을 띄울 때, 로그와 성능을 한곳에서 보고 어디부터 볼지 빨리 정하기 위한 감시 시스템입니다.

탐지의 정답은 규칙 플러그인입니다. AI는 추린 결과만 총평하며, 원본 로그 전체를 넣지 않습니다. AI가 꺼져 있거나 실패해도 수집·징후·규칙 보고서는 그대로 남습니다.

기동·반입·설정 파일 조작은 [README.md](README.md) 를 봅니다.

---

## 1. 왜 만들었는지

상용 APM·로그 플랫폼은 팀 단위 관제에 맞춰져 있습니다. 집이나 소규모 환경에서 서버를 여러 대 돌리면, 접속해서 로그를 열고 `top` 을 보는 일이 반복됩니다.

Guardian은 그 일을 한 화면으로 모읍니다.

- **대상**: 개인·소규모 운영자. 여러 호스트에 웹·앱·DB를 나눠 둔 경우.
- **목적**: 지금 위험한 서버가 어디인지, 로그 쪽인지 리소스 쪽인지 먼저 본다.
- **한계**: 실시간 SIEM이 아닙니다. 증분 수집과 규칙 매칭, 그다음 선택적 AI 브리핑입니다.

## 2. 하는 일

- **로그 감시**: 등록한 서버에서 로그를 읽어 이상징후(Finding)를 남긴다.
- **리소스 감시**: CPU·메모리·디스크·프로세스 생존을 모아 상태로 보여 준다.
- **전체 현황**: 감시 현황 첫 화면에서 로그 징후와 리소스를 한눈에 묶고, 켜 두면 AI 전체 총평을 한 번 붙인다.
- **보고서**: 서버별·일일 문서를 만들고, 켜 두면 AI 총평을 붙인다.

상태 태그는 **위험 / 주의 / 여유 / 대기** 입니다. 대기는 아직 수집이 없는 서버입니다.

## 3. 화면

| 메뉴 | 주소 | 하는 일 |
| --- | --- | --- |
| 현황 | `/` | 감시 현황. 전체 온도, 도넛·막대, 서버 카드, AI 전체 총평 |
| 서버 → 리소스 및 성능 수집 대상 서버 | `/servers/resources` | SSH·스크립트 등록, 접속 확인, **전체 수집하기** |
| 서버 → 로그 수집 대상 서버 | `/servers/logs` | SSH·로컬·로그 경로·플러그인 등록, **전체 수집하기** |
| 징후 | `/findings` | Finding 목록. 레벨·건수·서버 |
| 보고서 → 리소스 및 성능 보고서 | `/reports/resources` | 서버 박스 → 목록 → 상세. **전체 보고서 생성** |
| 보고서 → 로그 분석 보고서 | `/reports` | 서버 박스(ERROR·WARN·INFO) → 목록 → 상세. **전체 보고서 생성** |
| 플러그인 → 리소스 및 성능 쉘 스크립트 | `/plugins/resources` | 4단계 수집 스크립트 고르고 받기 |
| 플러그인 → 로그 분석 플러그인 | `/plugins/logs` | 1·2·3단계 규칙 보기·추가 |
| 설정 | `/settings` | 수집 주기, 일일 보고서 시각. 나머지는 `config.yaml` |

같은 이름으로 로그 서버와 리소스 서버를 등록하면, 감시 현황에서 한 장비의 로그와 리소스가 같이 보입니다.

## 4. 동작 흐름

```
서버 등록 (local / SSH)
        ↓
로그 수집 · 리소스 수집   ← 주기 스케줄, 또는 화면의 수집 / 전체 수집하기
        ↓
플러그인으로 징후·상태 판정
        ↓
감시 현황 (한눈에 보는 온도 + 서버 카드)
        ↓
보고서 (+ AI 총평, 선택)  ← 지정 시각, 또는 전체 보고서 생성 / 서버별 생성
```

백그라운드:

- 로그 수집은 `collect.interval_seconds` 마다 (기본 60초, 최소 15초).
- 로그 일일 보고서는 `scheduler.daily_report_time` 에 (기본 18:00, `app.timezone` 기준).
- 화면에서 누른 수집·보고서는 그 즉시 돌아갑니다.

## 5. 로그 수집 대상 등록

**서버 → 로그 수집 대상 서버 → 서버 등록하기**

1. 이름, 호스트, 포트, 계정, 인증(비밀번호 또는 개인키)을 넣습니다. 수집기는 `ssh` 또는 `local` 입니다.
2. 로그 경로는 줄바꿈 또는 쉼표로 여러 개 넣을 수 있습니다. `%Y-%m-%d` 같은 날짜 패턴은 최근 `lookback_hours`(기본 48시간) 구간으로 펼칩니다.
3. **1단계 공통 플러그인**에서 Java, Spring Boot, Nginx, DB처럼 여러 곳이 쓰는 로그 종류를 고릅니다. ERROR 집계·로그인 실패·디스크 부족은 항상 적용됩니다.
4. **2단계 세부 플러그인**은 이 서버에서만 볼 문구를 고릅니다. 플러그인은 로그 분석 플러그인 화면에서 만듭니다.
5. 저장 후 행의 **수집**, 또는 검색창 오른쪽 **전체 수집하기**로 읽습니다. 커서는 파일 끝 위치를 기억해 증분만 가져옵니다.

비밀번호는 암호화해서 보관하고, 올린 개인키는 서버 안의 데이터 디렉터리에만 둡니다.

## 6. 리소스 수집 대상 등록

**플러그인 → 리소스 및 성능 쉘 스크립트**에서 모듈을 고르고 `이름/collect.sh` zip 을 받습니다.

**서버 → 리소스 및 성능 수집 대상 서버 → 서버 등록하기**

1. 로그 서버와 **같은 이름**을 쓰면 현황에서 한 세트로 묶입니다.
2. 접속 정보와 수집 스크립트, 수집 경로를 넣습니다.
3. Guardian이 SSH로 붙어 스크립트를 돌리거나, 화면에서 일자별 JSON을 직접 넣을 수 있습니다.
4. 결과는 `DailyData/YYYY-MM-DD.json` 형태입니다. 박스에는 CPU · 메모리 · 디스크만 먼저 보입니다.

행의 **수집** 또는 **전체 수집하기**는 로그 수집과 별개입니다. 한 대가 실패해도 나머지는 계속 모읍니다.

## 7. 플러그인 단계

| 단계 | 역할 | 예 |
| --- | --- | --- |
| 1단계 공통 | 로그 레벨·스택별 1차 필터. 서버 등록 때 고름 | `common`, Java, Spring Boot, Python, Nginx, Apache, Tomcat, JEUS, WebLogic, MySQL, PostgreSQL, Oracle, Kafka, RabbitMQ, Redis, systemd, 로그인 실패, 디스크 부족 |
| 2단계 세부 | 이 서버만의 오류 문구 | 로그 분석 플러그인에서 추가한 규칙 |
| 3단계 보고서 | Finding을 문서로 묶음 | 일일 보고서, 서버 보고서 |
| 4단계 쉘 스크립트 | 리소스 JSON 수집 | `resource_basic` (CPU·메모리·디스크·인스턴스) |

새 로그 규칙은 화면에서 만들고, 로그 서버 수정의 2단계에 붙입니다. 새 리소스 스크립트는 모듈을 고르면 만들어집니다.

## 8. 감시 현황

첫 화면은 등록한 서버의 **로그와 리소스를 한눈에** 보여 줍니다.

- 가운데 도넛은 위험·주의·여유·대기 대수입니다.
- 리소스 / 로그 막대를 누르면 아래 해당 구역으로 이동합니다. 카드는 잠깐 강조됩니다.
- 서버 카드를 누르면 로그면 징후·보고서, 리소스면 해당 수집·보고서 화면으로 갑니다.
- 규칙 요약이 먼저 있고, OpenAI 또는 Dify가 켜져 있으면 **AI 전체 총평**이 붙습니다. 다시 만들기는 우측 작은 새로고침입니다. 원본 로그가 아니라 서버 이름·상태·문제 문구·건수만 전달합니다.

## 9. AI 총평이 붙는 시점

| 위치 | 언제 | 넣는 것 |
| --- | --- | --- |
| 감시 현황 AI 전체 총평 | 새로고침(또는 최초 생성) | 전체 서버의 상태 요약 |
| 로그 분석 보고서 | **전체 보고서 생성** 또는 서버 박스의 보고서 생성, 매일 지정 시각 | 그 서버 Finding 요약 |
| 리소스 및 성능 보고서 | **전체 보고서 생성** 또는 서버 박스의 보고서 생성 | CPU·메모리·디스크 추이 숫자 |

공통 규칙:

- `openai.enabled` + API 키, 또는 `dify.enabled` + 워크플로가 있어야 총평이 생깁니다.
- 둘 다 꺼져 있으면 규칙 문장만 나옵니다.
- 호출이 실패해도 수집 결과와 규칙 보고서는 유지됩니다.
- 프롬프트는 없는 원인·없는 수치를 만들지 말라고 못 박습니다.

## 10. 시연 시나리오

저장소의 `sample_logs/` 와 데모 시드로 정상과 이상을 같이 보여 줄 수 있습니다.

| 구분 | 보이는 것 |
| --- | --- |
| 이상 서버 | ERROR/WARN Finding, 카드 **위험** 또는 **주의**, 보고서 총평에 확인할 서버 |
| 정상 서버 | INFO 트래픽, 카드 **여유**, 「정상 로그 N건 · ERROR·WARN 없음」 |
| 미수집 | 카드 **대기**, 빈 CPU/로그 칸 → **전체 수집하기** 후 다시 현황 |

`app.seed_demo: true` 이면 기동 시 데모 서버와 샘플 로그가 들어갑니다. 반입·실데이터 환경은 `false` 로 둡니다.

권장 순서:

1. 로그·리소스 서버를 등록하고 접속이 초록인지 확인한다.
2. 각 목록에서 **전체 수집하기**를 누른다.
3. 현황에서 위험 카드를 눌러 징후를 연다.
4. 보고서 화면에서 **전체 보고서 생성**을 누른 뒤 박스를 열어 총평을 본다.

## 11. 데이터·설정 위치

| 위치 | 내용 |
| --- | --- |
| `config.yaml` | 기동 설정. 예시는 `config.example.yaml`. 다른 파일은 `GUARDIAN_CONFIG` |
| `data/guardian.db` | SQLite. 서버, 징후, 보고서 목록 |
| `data/resources/{서버}/{날짜}.json` | 리소스 일자별 원자료 |
| `data/reports/` | 보고서 HTML · Markdown |
| `data/overview_review.json` | 감시 현황 AI 전체 총평 캐시 |
| `plugins/` | 1~4단계 플러그인 |
| `sample_logs/` | 시연용 샘플 로그. 앱이 수집 경로로 읽을 수 있게 서버에 등록해서 씁니다 |

비밀번호·키 원문은 문서와 git에 넣지 않습니다. `config.yaml` 과 `data/` 는 반입 zip에 넣지 않습니다.

## 12. 구성

| 구분 | 내용 |
| --- | --- |
| 화면 | FastAPI + Jinja2, 기본 `http://127.0.0.1:8080` |
| 저장 | SQLite (`data/guardian.db`). 공용이면 PostgreSQL URL로 바꿀 수 있음 |
| 수집 | local 파일 또는 Paramiko SSH, 증분 커서 |
| 스케줄 | APScheduler (수집 주기 + 일일 보고서) |
| 로그 플러그인 | 1단계 공통 → 2단계 세부 → 3단계 보고서 |
| 리소스 플러그인 | 4단계 쉘 스크립트 |
| AI | OpenAI 또는 Dify. 요약만 전달 |

사내·폐쇄망은 `auth.enabled: true`, OpenAI 끄기, 망 안 Dify만 켜는 구성을 기본으로 합니다. 자세한 기동은 README의 폐쇄망 반입을 따릅니다.
